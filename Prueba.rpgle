     DPcia             s              3a   Inz('0 0')
     DKnr              s              2s 0 Inz(*Zeros)
      /Free
       Knr = %Scan(' ':Pcia);
       If Knr > *Zeros;
        Dsply Knr;
       Else;
        Dsply Knr;
       Endif;
       *Inlr = *On;
      /End-Free